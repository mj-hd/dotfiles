- default:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim
    ~/.config/ranger: ranger
    ~/.config/coc: coc
    ~/.zshrc: zshrc
    ~/.env: env
    ~/.zshfuncs: zshfuncs
